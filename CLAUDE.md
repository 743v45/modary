# Modary — 项目规范

## 探测方法

**二分法定位 Corpus 截止时间**：
- 向模型提问 2025 之后的事件，只能凭训练数据回答
- 禁止使用任何工具（搜索、联网等）
- 通过二分法逐步缩小范围，定位模型训练数据的截止节点
- 找到模型「知道」和「不知道」的边界日期

## 固定流程提示词

当用户要求记录一次模型对话时，严格按以下流程执行：

### 第 1 步：完整性校验

从用户贴的对话内容中提取以下字段。未提供的字段**禁止猜测**，必须向用户确认后才能继续。

| 字段 | 说明 | 示例 | 提取来源 |
|------|------|------|----------|
| `model` | 模型原名 | `claude-opus-4-7` | 对话头部/上下文 |
| `date` | 对话日期 | `2026-05-27` | 对话元数据或用户补充 |
| `corpus_cutoff` | 模型 Corpus 截止时间 | `2025-04-15` | 从二分法对话结论中提取 |
| `summary` | 截止时间的判定依据 | 二分法探测过程简述 | 从对话结论中提炼 |
| `conversation` | 原始对话全文 | — | 用户粘贴内容 |

**校验结果格式**（写入前必须输出）：

```
完整性检查:
  model:          ✅ claude-opus-4-7
  date:           ❓ 缺少 — 请补充对话日期
  corpus_cutoff:  ✅ 2025-04-15
  summary:        ✅ 二分法定位到...
  conversation:   ✅ 已提供
```

全部 ✅ 才能进入第 2 步。有任何 ❓ 必须等用户确认。

### 第 2 步：写入 raw

文件路径：`raw/{model}-{date}.md`

如果文件已存在，**追加**新条目（用 `---` 分隔），不覆盖。

格式模板：

```markdown
# {model} — {date}

**时间**: {date}
**模型**: {model}
**Corpus 截止**: {corpus_cutoff}

### 对话内容

{原始对话全文粘贴至此}

### 结论

{summary}

---
```

### 第 3 步：更新汇总表

文件路径：`CORPUS.md`

在表格末尾追加一行（含对话时间），备注列留空供用户自行填写。

### 第 4 步：确认

输出确认信息：

```
已记录:
- raw/{model}-{date}.md
- CORPUS.md
```

## 规则

- 模型名用原名，不用别名（`claude-opus-4-7` 不写 `claude`）
- 日期格式 `YYYY-MM-DD`
- raw 文件只追加，不修改历史记录
- 汇总表中的链接使用相对路径
- 项目只关注 Corpus 截止时间，不记录其他话题
- 探测时禁止使用任何工具，纯靠模型训练数据回答
